#include<bits/stdc++.h>
using namespace std;
int main(){
	//basic cin
	int v, n; cin>>v>>n;
	int A[n];
	for(int i=1;i<=n;i++) cin>>A[i];
	
	// turn everything to zero
	int dpm[n+1][v+1];
	for(int i=0;i<=n;i++){
		for(int j=0;j<=v;j++){
			dpm[i][j]=0;
		}
	}
	
	//main code
	for(int i=1;i<=n;i++){
		for(int j=1;j<=v;j++){
			if(j-A[i]>=0){
				dpm[i][j] = max(dpm[i-1][j], dpm[i-1][j-A[i]]+A[i]);//me take		
			}else{
				dpm[i][j]=dpm[i-1][j]; // me no take
			}
		}
	}

	cout<<v-dpm[n][v];	 //cout answer;
}
